schemaVersion: 2.2.2
metadata:
  name: intellij-interactive
  displayName: IntelliJ (Interactive Bypass)
  attributes:
    controller.devfile.io/devworkspace-config:
      workspace:
        progressTimeout: "15m"
components:
  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:ubi8-latest
      memoryLimit: 4Gi
      mountSources: true
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -e
          
          # 1. FIX IDENTITY
          if ! grep -q "1234" /etc/passwd; then
            echo "Fixing Identity for UID 1234..."
            echo "user:x:1234:0:User:${HOME}:/bin/bash" >> /etc/passwd
          fi
          
          # 2. CLEAR PORT 8887
          fuser -k 8887/tcp || true
          
          # 3. INSTALL PROJECTOR
          export PATH=$PATH:$HOME/.local/bin
          if ! command -v projector &> /dev/null; then
             echo "Installing JetBrains Projector..."
             pip3 install projector-installer --user
          fi
          
          # 4. PATCH PYTHON CRASH
          SITE_PACKAGES=$HOME/.local/lib/python3.11/site-packages
          TARGET_FILE="$SITE_PACKAGES/projector_installer/products.py"
          if [ -f "$TARGET_FILE" ]; then
             echo "Applying Patch..."
             if ! grep -q "from dataclasses import dataclass, field" "$TARGET_FILE"; then
                sed -i 's/from dataclasses import dataclass.*/from dataclasses import dataclass, field/' "$TARGET_FILE"
             fi
             sed -i 's/ver: LooseVersion = .*/ver: LooseVersion = field(default_factory=lambda: LooseVersion("0.0.0"))/' "$TARGET_FILE"
          fi
          
          # 5. INSTALL INTELLIJ (The Fix)
          if ! projector config show &> /dev/null; then
             echo "Downloading IntelliJ IDEA Community..."
             # Send '1' (Select version) then 'y' (Accept license)
             # We use printf to send exactly these two inputs
             printf "1\ny\n" | projector install IDEA_COMMUNITY
          fi
          
          # 6. FIND CONFIG NAME
          # We need to know what the installer named it (e.g. "IntelliJ_IDEA_Community_Edition_2023...")
          CONFIG_NAME=$(projector config show | grep "Config name:" | awk '{print $3}' | head -n 1)
          
          if [ -z "$CONFIG_NAME" ]; then
             echo "Error: Could not find installed config!"
             exit 1
          fi
          
          # 7. RUN IT
          echo "Launching config: $CONFIG_NAME"
          # Force host/port to ensure external access
          projector run "$CONFIG_NAME" --port 8887 --host 0.0.0.0
      endpoints:
        - name: intellij-direct
          targetPort: 8887
          attributes:
            public: "true"
            protocol: http
            secure: "false"
            discoverable: "true"
      env:
        - name: HOME
          value: /projects
commands:
  - id: run-app
    exec:
      commandLine: python app.py
      workingDir: ${PROJECT_SOURCE}
      component: tools
      group:
        kind: run
        isDefault: true
