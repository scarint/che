schemaVersion: 2.2.2
metadata:
  name: intellij-patch-fix
  displayName: IntelliJ (Python Patch)
  attributes:
    app.che.eclipse.org/editor: che-incubator/che-code/latest
components:
  - name: tools
    container:
      # Use the stable UDI image
      image: quay.io/devfile/universal-developer-image:ubi8-latest
      memoryLimit: 4Gi
      mountSources: true
      # --- THE STARTUP SCRIPT ---
      command: ["/bin/sh", "-c"]
      args:
        - |
          # 1. FIX IDENTITY (The "Victory" Patch)
          if ! grep -q "1234" /etc/passwd; then
            echo "Fixing Identity for UID 1234..."
            echo "user:x:1234:0:User:${HOME}:/bin/bash" >> /etc/passwd
          fi
          
          # 2. CLEAR PORT 8887
          fuser -k 8887/tcp || true
          
          # 3. INSTALL PROJECTOR (User Mode)
          export PATH=$PATH:$HOME/.local/bin
          if ! command -v projector &> /dev/null; then
             echo "Installing JetBrains Projector..."
             pip3 install projector-installer --user
          fi
          
          # 4. --- THE MAGIC PATCH ---
          # Fix the Python 3.11 compatibility error in products.py
          SITE_PACKAGES=$HOME/.local/lib/python3.11/site-packages
          TARGET_FILE="$SITE_PACKAGES/projector_installer/products.py"
          
          if [ -f "$TARGET_FILE" ]; then
             echo "Patching Projector source code at $TARGET_FILE..."
             # Step A: Add 'field' to the imports
             sed -i 's/from dataclasses import dataclass/from dataclasses import dataclass, field/' "$TARGET_FILE"
             # Step B: Replace the broken mutable default with a default_factory
             sed -i 's/ver: LooseVersion = LooseVersion("0.0.0")/ver: LooseVersion = field(default_factory=lambda: LooseVersion("0.0.0"))/' "$TARGET_FILE"
          else
             echo "WARNING: Could not find products.py to patch!"
          fi
          # ---------------------------
          
          # 5. INSTALL INTELLIJ
          if ! projector config show IntelliJ &> /dev/null; then
             echo "Downloading IntelliJ IDEA Community..."
             projector install --name IntelliJ --ide IDEA_COMMUNITY --port 8887 --accept-license --hostname 0.0.0.0
          fi
          
          # 6. RUN IT
          echo "Starting IntelliJ Projector..."
          projector run IntelliJ
      # --------------------------
      endpoints:
        - name: intellij-direct
          targetPort: 8887
          attributes:
            public: "true"
            protocol: http
            secure: "false"
            discoverable: "true"
      env:
        - name: HOME
          value: /projects
commands:
  - id: run-app
    exec:
      commandLine: python app.py
      workingDir: ${PROJECT_SOURCE}
      component: tools
      group:
        kind: run
        isDefault: true
