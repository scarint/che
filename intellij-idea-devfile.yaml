schemaVersion: 2.2.2
metadata:
  name: intellij-config-patch
  displayName: IntelliJ (Config Patch)
  attributes:
    controller.devfile.io/devworkspace-config:
      workspace:
        progressTimeout: "15m"
components:
  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:ubi8-latest
      memoryLimit: 4Gi
      mountSources: true
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -e
          
          # 1. FIX IDENTITY
          if ! grep -q "1234" /etc/passwd; then
            echo "Fixing Identity for UID 1234..."
            echo "user:x:1234:0:User:${HOME}:/bin/bash" >> /etc/passwd
          fi
          
          # 2. CLEAR PORT
          fuser -k 8887/tcp || true
          
          # 3. INSTALL PROJECTOR
          export PATH=$PATH:$HOME/.local/bin
          if ! command -v projector &> /dev/null; then
             echo "Installing JetBrains Projector..."
             pip3 install projector-installer --user
          fi
          
          # 4. PATCH PYTHON
          SITE_PACKAGES=$HOME/.local/lib/python3.11/site-packages
          TARGET_FILE="$SITE_PACKAGES/projector_installer/products.py"
          if [ -f "$TARGET_FILE" ]; then
             if ! grep -q "from dataclasses import dataclass, field" "$TARGET_FILE"; then
                echo "Applying Python 3.11 Patch..."
                sed -i 's/from dataclasses import dataclass.*/from dataclasses import dataclass, field/' "$TARGET_FILE"
                sed -i 's/ver: LooseVersion = .*/ver: LooseVersion = field(default_factory=lambda: LooseVersion("0.0.0"))/' "$TARGET_FILE"
             fi
          fi
          
          # 5. INSTALL INTELLIJ
          if ! projector config show &> /dev/null; then
             echo "Navigating Installer Menu..."
             printf "y\ny\n5\n" | projector install IDEA_COMMUNITY
          fi
          
          # 6. --- THE CONFIG PATCH (Force 0.0.0.0:8887) ---
          # We manually edit the INI file because CLI flags are being ignored
          CONFIG_INI="$HOME/.projector/configs/IntelliJ/config.ini"
          
          if [ -f "$CONFIG_INI" ]; then
             echo "Patching Projector Config at $CONFIG_INI..."
             # Replace PORT=... with PORT=8887
             sed -i 's/PORT = .*/PORT = 8887/' "$CONFIG_INI"
             # Replace HOST=... with HOST=0.0.0.0
             sed -i 's/HOST = .*/HOST = 0.0.0.0/' "$CONFIG_INI"
             
             # Verify patch
             grep "PORT =" "$CONFIG_INI"
             grep "HOST =" "$CONFIG_INI"
          else
             echo "WARNING: Config INI not found at $CONFIG_INI"
          fi
          # ------------------------------------------------
          
          # 7. RUN IT
          echo "Launching IntelliJ..."
          projector run IntelliJ
      endpoints:
        - name: intellij-direct
          targetPort: 8887
          attributes:
            public: "true"
            protocol: http
            secure: "false"
            discoverable: "true"
      env:
        - name: HOME
          value: /projects
commands:
  - id: run-app
    exec:
      commandLine: python app.py
      workingDir: ${PROJECT_SOURCE}
      component: tools
      group:
        kind: run
        isDefault: true
