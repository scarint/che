schemaVersion: 2.2.2
metadata:
  name: intellij-port-fix
  displayName: IntelliJ (Port 9999)
  attributes:
    controller.devfile.io/devworkspace-config:
      workspace:
        progressTimeout: "15m"
components:
  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:ubi8-latest
      memoryLimit: 4Gi
      mountSources: true
      # --- NETWORK CONFIG ---
      # The server stubbornly wants 9999. We expose 9999.
      endpoints:
        - name: intellij-direct
          targetPort: 9999
          attributes:
            public: "true"
            protocol: http
            secure: "false"
            discoverable: "true"
      # ----------------------
      env:
        - name: HOME
          value: /projects
      command: ["/bin/sh", "-c"]
      args:
        - |
          set -e
          
          # 1. FIX IDENTITY
          if ! grep -q "1234" /etc/passwd; then
            echo "Fixing Identity for UID 1234..."
            echo "user:x:1234:0:User:${HOME}:/bin/bash" >> /etc/passwd
          fi
          
          # 2. INSTALL PROJECTOR
          export PATH=$PATH:$HOME/.local/bin
          if ! command -v projector &> /dev/null; then
             echo "Installing Projector..."
             pip3 install projector-installer --user
          fi
          
          # 3. PATCH PYTHON (Required for 3.11)
          SITE_PACKAGES=$HOME/.local/lib/python3.11/site-packages
          TARGET_FILE="$SITE_PACKAGES/projector_installer/products.py"
          if [ -f "$TARGET_FILE" ]; then
             if ! grep -q "from dataclasses import dataclass, field" "$TARGET_FILE"; then
                echo "Applying Python 3.11 Patch..."
                sed -i 's/from dataclasses import dataclass.*/from dataclasses import dataclass, field/' "$TARGET_FILE"
                sed -i 's/ver: LooseVersion = .*/ver: LooseVersion = field(default_factory=lambda: LooseVersion("0.0.0"))/' "$TARGET_FILE"
             fi
          fi
          
          # 4. INSTALL INTELLIJ
          if ! projector config show &> /dev/null; then
             echo "Installing IntelliJ..."
             printf "y\ny\n5\n" | projector install IDEA_COMMUNITY
          fi
          
          # 5. RUN IT
          echo "Launching IntelliJ..."
          
          # We force HOST to 0.0.0.0 so external traffic can reach it.
          # We let PORT default to 9999, since that is what it wants to do anyway.
          projector run "IntelliJ" --host 0.0.0.0
